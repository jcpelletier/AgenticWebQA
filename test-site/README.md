# Test Site

Static dummy website used as a stable target for UI and browser automation tests.
It is intentionally simple and local-only so test flows can run without external dependencies.

## Usage

- Open `test-site/index.html` directly in a browser for a quick manual check.
- Or serve the folder with any static server if your tests require `http://` URLs.
